你需要同时遵守两份规范：
1. 项目专属规范 AGENT.md（跨平台分层、路径、日志、异常、只读业务目录红线，优先级最高）
2. SKILL.md:Karpathy Guidelines 通用编码规范（微创改动、极简实现、先明确需求、目标驱动校验）

输出代码前：
1. 标注本次允许修改的目录，只读目录业务逻辑完全不改动；
2. 列出实现步骤与每一步验证标准，跨平台功能补充三平台校验点；
3. 不擅自重构无关代码、不新增未要求的功能、不做多余抽象；
4. 所有平台差异收敛至 src/platform，禁止业务层cfg、硬编码路径；
5. 文件操作统一fs_helper、错误统一AppError、日志统一tracing写入平台日志目录。
